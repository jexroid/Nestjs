<!DOCTYPE html>
<html lang="fa">

<head>
    <meta charset="UTF-8">
    <title> ورود </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet"/>
    <link rel="stylesheet" href="/css/login.css">
</head>

<body class="bg-dark text-white">
    <div id="particles-js" ></div>
    <div class="container h-100">
        <div class="row justify-content-center align-items-center h-100">
            <div class="col-lg-8 col-md-8 col-sm-12 col-12">
                <div class="multi-step-form">
                    <h6 class="text-center mb-4 animate__animated animate__fadeInDown">شماره تماس را وارد کنید</h6>
                    <div class="mb-3">
                        <input id="phoneNumber" type="text" class="form-control" placeholder="شماره تماس" min="1000000000" max="9999999999">
                    </div>
                    <div class="mb-3">
                        <input id="smsCode" type="text" class="form-control" placeholder="کد پیامک" pattern="\d{4}" disabled>
                    </div>
                    <div>
                        <button class="btn btn-primary flex-fill " id="submitBtn" type="button" disabled>ورود</button>
                        <button class="btn btn-secondary flex-fill" id="resendCodeBtn" type="button" disabled><small>ارسال مجدد کد</small></button>
                    </div>
                    
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
     <script src="path_to_particles.js"></script> <!-- این خط باید حذف شود، مگر اینکه نسخه سفارشی شده‌ای داشته باشید -->

    <script>
        
        const phoneNumberInput = document.getElementById("phoneNumber");
        const smsCodeInput = document.getElementById("smsCode");
        const submitBtn = document.getElementById("submitBtn");
        const resendCodeBtn = document.getElementById("resendCodeBtn");

        phoneNumberInput.addEventListener('input', handlePhoneNumberChange);
        smsCodeInput.addEventListener('input', handleSmsCodeChange);
        submitBtn.addEventListener('click', handleSubmit);
        resendCodeBtn.addEventListener('click', handleResend);

        function handlePhoneNumberChange() {
            smsCodeInput.disabled = this.value.length !== 11;
            resendCodeBtn.disabled = this.value.length !== 11;
        }

        function handleSmsCodeChange() {
            submitBtn.disabled = this.value.length !== 4;
        }

            function handleSubmit() {
        if (smsCodeInput.value === '1234') {
            window.location.href = "./footer.html";
        } else {
            Swal.fire({
                title: 'خطا!',
                text: 'کد وارد شده نادرست است یا شما ثبت‌نام نکرده‌اید.',
                icon: 'error',
                confirmButtonText: 'باشه'
            });
        }
    }

    function handleResend() {
    Swal.fire({
        title: 'توجه!',
        text: 'کد ارسال شد.',
        icon: 'info',
        confirmButtonText: 'باشه'
    });
}

       
          
      
    </script>
</body>

</html>
